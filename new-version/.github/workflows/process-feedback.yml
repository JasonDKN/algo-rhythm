name: Process Digest Feedback

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  process:
    if: contains(github.event.issue.labels.*.name, 'digest-feedback')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Process feedback
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: python feedback_processor.py

      - name: Commit updated taste profile
        run: |
          git config user.name "content-digest-bot"
          git config user.email "actions@github.com"
          git add data/taste_profile.json
          git diff --quiet --cached || git commit -m "Update taste profile from feedback [skip ci]"
          git push
